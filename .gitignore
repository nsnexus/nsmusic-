# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (M-24 no AUDIT_REPORT.md: .env sozinho não estava coberto, só .env*.local)
.env
.env*.local

# vercel
.vercel

# wrangler (workers/efi-proxy/) — estado local e secrets de dev, nunca versionar
.wrangler/
.dev.vars
.dev.vars.*

# certificado mTLS da Efí (docs/EFI_SETUP.md) — nunca versionar
*.p12
*.pem

# typescript
*.tsbuildinfo
next-env.d.ts

# logs de migrações executadas manualmente (scripts/*.mjs)
/scripts/migration-logs/
